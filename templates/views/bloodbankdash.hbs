<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/prerequisite.css">
    <link rel="stylesheet" href="/css/nav.css">
    <link rel="stylesheet" href="/css/bloodbankdash.css">
    <title>Login</title>
    <script src="../js/bloodbankdash.js" defer></script>
</head>
<body>
    <nav>
      <div class="nav-container">
        <a href="../"><div class="logo-container">
          <div class="logo"></div>
          <h1>Blood Care</h1>
        </div>
        </a>
        <ul class="primary-nav-items">
          <li><a href="/logout"><button>Logout</button></a></li>
        </ul>
      </div>
    </nav>
    <main>
      <div class="container">
        <h1>Welcome {{ user }}</h1>
      </div>
       <div class="user-container" section id="vol-sect">
        <ul class="volunt container">
            {{#each result}}
            <li class="vol">
                <span class=" vol-i number">{{this.Name}}</span>
                <span class=" vol-i name">{{this.Email}}</span>
                <span class=" vol-i location">{{this.Bgroup}}<i class="fa fa-tint" aria-hidden="true"></i></span>
                <span class=" vol-i blood group">{{this.PhNo}}</span>
            </li>
            {{/each}}
        </ul>
    </div>
    </main>
</body>
</html>